package net.company;

/**
 * Common utilities and global constants.
 */
public class UTUtil {
    /** Used when comparing two doubles. Two doubles with diff larger than MAX_DIFF are treated not equal. */
    public static double MAX_DIFF = 0.0001;
}
